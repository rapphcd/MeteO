# METEO VIEWER

A small, dynamic website using the "flask" module, allowing users to access the current weather by entering a city. It works by querying the OpenWeatherMap API with the module "requests".